# Optional end-to-end tests (e.g. Playwright) against a deployed preview or staging.
# Enable when you have a stable URL; add repository secrets such as E2E_BASE_URL.
name: E2E

on:
  workflow_dispatch:
  # pull_request:
  #   branches: [staging]

permissions:
  contents: read

jobs:
  placeholder:
    name: E2E placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: |
          echo "Add Playwright (or Cypress) and point E2E_BASE_URL at staging when ready."
          echo "See README or SUPPORT.md for required secrets."
